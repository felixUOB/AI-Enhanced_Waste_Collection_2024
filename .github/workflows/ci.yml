name: CI

on:
  push:

jobs:
  flutter_test:
    name: Run Flutter test and analyze
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "./ewc_frontend"    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Setup and Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
      - name: Secrets
        run: |
          touch .env
          echo API_KEY=${API_KEY} >> .env
        env: 
          api_key: ${{ secrets.API_KEY }}

      - name: Run format, analyze, test
        working-directory: "./ewc_frontend"  
        run: |
          flutter pub get
          flutter analyze
          flutter test

  django_test:
    name: Run Django Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Set environment variables
        run: |
          echo "DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY}" >> ewc_backend/.env
          echo "POSTGRES_DB=${POSTGRES_DB}" >> ewc_backend/.env
          echo "POSTGRES_USER=${POSTGRES_USER}" >> ewc_backend/.env
          echo "POSTGRES_PASSWORD=${POSTGRES_PASSWORD}" >> ewc_backend/.env
          echo "POSTGRES_HOST=${POSTGRES_HOST}" >> ewc_backend/.env
          echo "POSTGRES_PORT=${POSTGRES_PORT}" >> ewc_backend/.env
          echo "SENDGRID_SECRET_KEY=${SENDGRID_SECRET_KEY}" >> ewc_backend/.env
        env:
            DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY }}
            POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
            POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
            POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
            POSTGRES_HOST: ${{ secrets.POSTGRES_HOST }}
            POSTGRES_PORT: ${{ secrets.POSTGRES_PORT }}
            SENDGRID_SECRET_KEY: ${{ secrets.SENDGRID_SECRET_KEY }}

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r ewc_backend/requirements.txt

      - name: Run migrations
        run: |
          cd ewc_backend
          python manage.py migrate

      - name: Run Django tests with coverage
        run: |
          cd ewc_backend
          coverage run manage.py test ewc_core.tests --noinput
          coverage report -m
          coverage html

      - name: Upload Coverage HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: django-coverage-html
          path: ewc_backend/htmlcov


  build:
    name: Build app
    needs: [flutter_test]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: "./ewc_frontend" 
    strategy:
      matrix: 
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Checkout Respository 
        uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with: 
          distribution: 'temurin'
          java-version: "17.x"

      - name: Configure Flutter environment
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Secrets
        run: |
          touch .env
          echo API_KEY=${API_KEY} >> .env
        env: 
          api_key: ${{ secrets.API_KEY }}
      
      - name: Build Android app
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: |
          echo "Building Android..."
          pwd
          flutter pub get
          flutter build apk --no-pub
          flutter build appbundle --no-pub 
      
      - name: Build iOS app
        if: ${{ matrix.os == 'macos-latest' }}
        run: |
          echo "Building iOS..."
          flutter pub get
          flutter clean
          flutter build ios --no-codesign

  # build-and-push:
  #   name: Build and Push Backend Docker Image
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v3

  #     - name: Configure AWS Credentials
  #       uses: aws-actions/configure-aws-credentials@v2
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: eu-north-1

  #     - name: Login to Amazon ECR
  #       id: login-ecr
  #       uses: aws-actions/amazon-ecr-login@v1

  #     - name: Build and Tag Docker Image
  #       run: |
  #         REPO_URI=713881812987.dkr.ecr.eu-north-1.amazonaws.com/ewc-django-backend
  #         IMAGE_TAG=$(git rev-parse --short HEAD)

  #         cd ewc_backend

  #         docker build -t $REPO_URI:$IMAGE_TAG .
  #         docker tag $REPO_URI:$IMAGE_TAG $REPO_URI:latest

  #     - name: Push Docker Image to AWS ECR
  #       run: |
  #         REPO_URI=713881812987.dkr.ecr.eu-north-1.amazonaws.com/ewc-django-backend
  #         IMAGE_TAG=$(git rev-parse --short HEAD)

  #         docker push $REPO_URI:$IMAGE_TAG
  #         docker push $REPO_URI:latest

  #     - name: Output Image URI
  #       run: echo "Image pushed to $REPO_URI:latest"
        