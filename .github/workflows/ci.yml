name: CI

on:
  push:

jobs:
  flutter_test:
    name: Run Flutter test and analyze
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "./ewc_frontend"    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Setup and Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
      - name: Secrets
        run: |
          touch .env
          echo API_KEY=${API_KEY} >> .env
        env: 
          api_key: ${{ secrets.API_KEY }}

      - name: Run format, analyze, test
        working-directory: "./ewc_frontend"  
        run: |
          flutter pub get
          flutter analyze
          flutter test

  build:
    name: Build app
    needs: [flutter_test]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: "./ewc_frontend" 
    strategy:
      matrix: 
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Checkout Respository 
        uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with: 
          distribution: 'temurin'
          java-version: "17.x"

      - name: Configure Flutter environment
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Secrets
        run: |
          touch .env
          echo API_KEY=${API_KEY} >> .env
        env: 
          api_key: ${{ secrets.API_KEY }}
      
      - name: Build Android app
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: |
          echo "Building Android..."
          pwd
          flutter pub get
          flutter build apk --no-pub
          flutter build appbundle --no-pub 
      
      - name: Build iOS app
        if: ${{ matrix.os == 'macos-latest' }}
        run: |
          echo "Building iOS..."
          flutter pub get
          flutter clean
          flutter build ios --no-codesign

  # build-and-push:
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v4

  #     - name: Configure AWS Credentials
  #       uses: aws-actions/configure-aws-credentials@v2
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: us-east-1  # Change to your AWS region

  #     - name: Login to Amazon ECR
  #       id: login-ecr
  #       uses: aws-actions/amazon-ecr-login@v1

  #     - name: Build and Tag Docker Image
  #       run: |
  #         REPO_URI=123456789012.dkr.ecr.us-east-1.amazonaws.com/my-repository  # Change this
  #         IMAGE_TAG=$(git rev-parse --short HEAD)

  #         docker build -t $REPO_URI:$IMAGE_TAG .
  #         docker tag $REPO_URI:$IMAGE_TAG $REPO_URI:latest

  #     - name: Push Docker Image to AWS ECR
  #       run: |
  #         REPO_URI=123456789012.dkr.ecr.us-east-1.amazonaws.com/my-repository  # Change this
  #         IMAGE_TAG=$(git rev-parse --short HEAD)

  #         docker push $REPO_URI:$IMAGE_TAG
  #         docker push $REPO_URI:latest

  #     - name: Output Image URI
  #       run: echo "Image pushed to $REPO_URI:latest"
        